# 2026-08-13

### 战机选择界面：每张卡片显示 3D 造型 + 排版优化（12:24）
- 需求：用户要求「排版更合理，每个飞机能看到造型」。
- 实现：每张 `.ship-card` 顶部加 `<canvas class="shipPrev">`；`HUD.initPreview()` 懒创建**单个共享 WebGLRenderer**（离屏，alpha，240x168，pixelRatio≤2）+ 预览场景（环境光+key/rim/fill 三向光）+ 相机(fov42, pos(3,1.9,4.3)) + pivot；用 `Gfx.ship(s.model, s.color, 1.0)` 建每架飞机，经 `Box3.setFromObject` 居中 + 统一缩放到 TARGET=2.5 以保证不同机型都框得下。
- `HUD.renderPreviews(now)`：每帧 `pivot.rotation.set(0.26, now*0.0005, 0)` 旋转，逐架 `renderer.render` 后 `ctx.drawImage(renderer.domElement,...)` 画进各卡片 2D canvas；CSS 径向渐变背景透出作底。
- 钩子：`Game.frame` 内 `if (this.state === 'MENU') HUD.renderPreviews(now);`（仅菜单态渲染，不耗游戏帧）。
- CSS：`.ship-cards` 改为带面板背景/边框的圆角容器(max-width 968px，居中换行)；卡片 132px、模型画布用 `aspect-ratio:240/168` 防拉伸；选中卡片给画布加青色描边发光；`.sdesc` 固定最小高度对齐。
- 校验：`node --check p8` 通过；`tools/build.mjs` 重建 `index.html` **198.2 KB**。puppeteer 仍不可用，实际观感待双击 index.html 确认（重点：11 架飞机建模是否都正常框入画幅、旋转是否顺滑）。
- 经验：离屏预览用「单 renderer + drawImage 到 2D canvas」而非每卡一个 WebGLRenderer，避免 WebGL 上下文数爆上限（本项目已占 1 个主 renderer，预览复用 1 个即可）。

### 战机选择界面缩小 + 去圆光晕（12:58 反馈后，13:21 完成）
- 用户反馈：预览"占整个屏幕、太大"，且 shipPrev 上的椭圆径向光晕（圆圆的）不好看。
- 修复：p1 CSS 卡片宽 132→106px、ship-cards max-width 968→744px（约 6/行更紧凑）；shipPrev 改 `aspect-ratio:5/4` + 扁平深色渐变，删除 `radial-gradient(ellipse …)` 圆光晕。
- p8：卡片 canvas 属性 240×168→200×160 与 CSS 比例一致防拉伸；`initPreview` TARGET 2.5→1.9、相机拉远 (3,1.9,4.3)→(3.2,1.9,5.4) 让飞机在缩略图里留白不撑满。
- 重建 198.2 KB 通过；puppeteer 仍不可用，需人工双击确认缩略图框幅与观感。

### 修复菜单态"全屏大图"（13:23 反馈）
- 根因：主游戏画布 `#gl`（`World.renderer.domElement`，`p3_world.js:22` insertBefore 到 body、铺满全屏）在菜单态仍渲染 3D 场景并透过半透明遮罩显示成"全屏大图"。战机预览用的是独立离屏 WebGLRenderer，与此无关。
- 修复：p1 CSS 新增 `#gl{position:fixed;inset:0;z-index:0;display:none}`（默认隐藏，启动不闪）；`HUD.showMenu()` 末尾 `World.renderer.domElement.style.display='none'`；`Game.start()` 进入 PLAYING 时置 `'block'`。结算/暂停/三选一态保留 #gl 可见（看战场），仅菜单态隐藏。
- 重建 198.5 KB 通过。puppeteer 不可用，需人工双击确认菜单只剩遮罩+11 架缩略图、无全屏 3D 场景。

### 主游戏画布改为菜单态从 DOM 摘掉（13:28 反馈）
- 用户要求"移除主游戏画布看看效果"。说明 `#gl` 即实际战斗 3D 画面，不能真删（否则进游戏黑屏）。
- 改为：菜单态 `HUD.showMenu()` 里 `gl.parentNode.removeChild(gl)` 真正从 DOM 摘除；`Game.start()` 进入 PLAYING 时 `document.body.insertBefore(gl, body.firstChild)` 挂回并 `display:block`。CSS 仍保留 `#gl{...display:none}` 作启动兜底。
- WebGL 上下文与 canvas 元素绑定、与 DOM 位置无关，摘挂不丢上下文，安全可逆。结算/暂停/三选一态保留 #gl 可见。
- 重建 198.6 KB 通过。需人工双击确认：菜单=纯遮罩+11 缩略图；点开始=全屏战斗画面正常。

### 修正：全屏"大图"真正根因 = 全局 canvas 规则（13:34 二次反馈）
- 上一轮误判为 #gl（其实 #gl 有 `#gl{position:fixed;inset:0;z-index:0;display:none}` id 规则压住、且在遮罩后）。**真正根因**：`p1_shell.html` 第17行全局 `canvas{display:block;position:absolute;inset:0}` 把页面里所有 <canvas> 顶成全屏——含 11 张 `.shipPrev` 战机预览（层层叠在最上那张盖住一切）和 `#minimap` 小地图。
- 修复：全局规则改为 `canvas{display:block}`（去掉 position:absolute;inset:0）；各 canvas 用自身规则定位（#gl fixed 全屏 / .shipPrev 卡片内流式 / #minimap 右下 150×150）。重建 198.6 KB 通过。
- 教训：新增 <canvas> 元素时，全局 canvas 定位规则会误伤，必须显式给每个 canvas 定 position，或把全局规则收窄到 #gl。
- 注：#gl 菜单态 detach/reattach 改动保留（无害、且满足用户"移除主画布看效果"诉求），但非此次全屏 bug 的关键。


### 修复开始/结算遮罩标题与开始按钮被顶出屏幕（13:57 反馈）
- 现象：去掉全屏 canvas 后菜单露出真容，但标题(星陨幸存者)与开始按钮被挤出视口、看不到。
- 根因：#overlay 用 display:flex;flex-direction:column;justify-content:center，内容高度(sTitle+sSub+howto+11 张缩略图 2 行+btnRow+credit)超过视口时，flex 居中把顶部标题推到屏幕上方外、底部开始按钮推到下方外，只露中间卡片。此前被全屏 canvas 盖住，修 canvas 后才暴露。
- 修复：#overlay 改为 justify-content:safe center; overflow-y:auto; padding:26px 16px 22px。safe 在溢出时回退 start(不裁切)，overflow-y:auto 允许内部滚动；内容够短仍居中。重建 203.4 KB 通过。
- 注：safe 不支持时该值被忽略回退 flex-start，同样不裁切，安全。


### 修复"重锤没建模"（14:09 反馈）
- 现象：选择界面/游戏中重锤(bulwark)看不到造型。
- 根因：`bulwark` 的 `model:'hauler'`，但 `assets/meshes.js` 从未烘焙过 `hauler` key（可用 key 仅 fighter/wing_a/wing_b/enemy_*/drone/blade/wasp/mender/phaser/boss_*）。`Gfx.parts` 取不到 → `Gfx.ship` 静默降级成空 Group（build/p2_core.js:187 `if (!parts) return {g,mats}`），故重锤渲染为空壳。`Gfx.meshYaw` 里虽有 `hauler:Math.PI/2` 遗留项，但网格缺失使其无意义。
- 修复：在 `tools/bake_models.mjs` 的 JOBS 新增 `{key:'hauler', file:K+'craft_cargoA.glb', size:2.1, rot:[0,PI,0]}`（用 craft_cargoA 货运船，区别于 titan 用的 craft_cargoB）；并把 `Gfx.meshYaw.hauler` 由遗留的 PI/2 改为 0（与其它烤期已对齐 +Z 的飞船一致）。重跑 `node tools/bake_models.mjs`（meshes.js 现 135KB / 17 key），再 `tools/build.mjs` 重建 index.html 198.6KB。
- 校验：meshes.js 可解析、hauler 4 部件、bulwark model resolves=true；p2 语法 OK。
- 教训：新增 SHIPS 条目时 `model` 必须对应一个已烘焙的 MESHES key，否则静默空壳（不报错难发现）；建议给 `Gfx.parts` 缺失 key 时 `console.warn` 以便暴露。


### 交付：源码+策划书整合 MD（14:32 请求）
- 用户需要可交付第三方评审优化的整合文档。生成 `类土豆兄弟_源码与策划书.md`（235 KB）。
- 结构：第一部分策划书（背景/玩法/架构表/多星域/13武器/11战机/10敌人/5僚机/BOSS/视觉音效/当前状态/已知技术债7条/待优化点9方向）；第二部分完整源码（p1_shell.html~p8_game.js 8 分片逐字内联）；第三部分构建与建模工具链（tools/build.mjs + tools/bake_models.mjs），meshes.js 因 135KB base64 仅说明不内联。
- 用 Python 读取真实文件拼接避免手抄；生成后删除一次性 assemble_doc.py。
- 技术债重点供评审：无尽模式 BOSS 只刷一次(nextBossRound 未调用)、缺失网格静默空壳、全局单单例耦合、puppeteer 验证环境损坏、#gl 菜单态摘除脆弱、飞锯/激光特殊约束。

### 任务 #96/#97/#98（已交付）：报告并入 + 核查清单 + 第五章技术债修复（15:18）
- 用户把第三方《星陨幸存者·性能优化与内容扩展报告》与源码整合，全选 4 项：①并报告入整合 MD ②逐条核查+优先级清单 ③落实第五章技术债 ④敌人 InstancedMesh 化。
- ① 报告作第四部分并入 `类土豆兄弟_源码与策划书.md`（269KB）：策划/源码/工具链/评审报告/核查清单(5.1 逐条表/5.2 退化发现/5.3 路线图/5.4 风险/5.5 执行顺序)。
- ③ 第五章两处修复：p2 `Gfx.ship`/`Gfx.enemyShip` 缺失 key 改走 `_mkMissingProxy(key)`（洋红自转方块 + `console.warn('[Gfx] Critical: Missing mesh key')` 一次）；p7 `Boss.spawn(round)` 加 `this.round/maxHp` 轮次强化，`Boss.die` 无尽态 `setTimeout→Game.nextBossRound()` 接入 BOSS 链（每轮 +45% 血）。p8 `Boss.spawn(this.bossRound)` 传轮次。
- 重建 index.html 204.8KB，node --check 全过，grep 确认 `_mkMissingProxy`/`Missing mesh key`/`下一轮来袭`/`Boss.spawn(this.bossRound)` 进产物。

### 任务 #99（完成）：敌人/经验水晶/子弹 InstancedMesh 化（15:43）
- 关键发现：`build/_index_old_backup.html`(120KB 旧备份) 已实现子弹+水晶 InstancedMesh（现版加内容时回退成独立 Mesh），可 harvest 其 `_writeInstanced`/`Loot.inst` 模式。
- **子弹(p4_player.js)**：玩家弹 pInst(420)/敌弹 eInst(300) InstancedMesh + 纯数据池 + `_flush(inst,pool,isPlayer)` 用 `setMatrixAt`+`setColorAt`(青/红) 写矩阵/色；导弹保留 Mesh 池（需独立朝向+尾焰）。
- **水晶 Loot(p6)**：`Loot.inst = InstancedMesh(OctahedronGeometry, MeshBasicMaterial, 400)` + 手动 `instanceColor`；`_put` 只写数据；`update` 用 `each` 做初速衰减/磁吸/拾取，再统一写矩阵+instanceColor（xp 蓝/绿/紫、hp 粉）；`clear→releaseAll+inst.count=0`。
- **敌人 Enemies(p6)**：新增 `Gfx.enemyBodyGeo(vk)` 把 `Mesh.parts(vk)` 全部 body 部件(去描边壳/尾焰)合并成单几何(position+normal)；`init` 按 `SPEC[].variants` 去重建 10 个 variant + `__default` 兜底 InstancedMesh（`MeshLambertMaterial` 白色 + `instanceColor`，每容量 300，frustumCulled=false，DynamicDrawUsage）。spawn 去掉 `e.mesh/e.mats/_mkMesh`，只设 `modelKey`+`color`(精英暖金/星域染色/种类)。update 去掉逐实例 mesh 写，末尾 `_flush()` 按 modelKey 分桶写矩阵(含受击闪白 lerp 到白、orbiter/splitter 自旋、bob 浮动)与 instanceColor；kill 不再 `e.mesh.visible=false`(回收后 _flush 自然消失)；clear `releaseAll`+重置 count。
- 校验：p2/p4/p6 三文件 + 全部 7 分片 `node --check` 全过；`tools/build.mjs` 重建 index.html **204.5KB**；grep 产物：enemy mesh 残留 0、`InstancedMesh` 14 处、`_insts` 7、`enemyBodyGeo` 2。
- ⚠️ 未运行时验证：puppeteer 无头环境损坏。人工双击 index.html 须确认：①多部件敌人(合并几何)观感/朝向正确 ②受击闪白 instanceColor ③星域染色/精英暖金 ④水晶磁吸拾取 ⑤BOSS 链无尽轮次加血。`Gfx.enemyShip` 现已无调用方(残留死代码，无害)。

### 任务 #100（完成）：整合文档同步至最新代码状态（16:20）
- 用户要求「更新 `类土豆兄弟_源码与策划书.md`」——即把 InstancedMesh 改造 + 第五章修复同步进交付文档。最终 **277.8 KB**。
- **新建 `tools/sync_doc.py`（可复用/幂等）**：按 `^# 第N部分` H1 切分文档 → 保留文档标题+第一部分/第四部分/第五部分手写文字原样 → 仅用磁盘真实文件重建第二部分(8 分片)与第三部分(build.mjs+bake_models.mjs)代码块 → 回写。第三部分开头 meshes 说明用 `^## tools/` 定位切出保留。已验证 **MD5 前后一致（幂等）**，且 10 个源文件全部「逐字包含」校验通过。
- 文档手写部分更新：①头部导语改列 5 部分 + 当前版本(204.5KB/实例化说明) + 提示可用 sync_doc.py 重同步；②新增 **§4.3 实例化渲染架构**（三实体载体/几何/容量/颜色对照表 + `_flush` 约定 + 导弹例外 + draw call 收益），原 §4.3 对象池顺延为 §4.4（补 Pool API）；③§5.4 敌人加实例化渲染段；④§5.6 BOSS 加无尽链公式；⑤§6 当前状态 +3 条 ✅；⑥§7 技术债把 BOSS 链/缺失网格标 ~~删除线~~+✅已修复，**新增第 7 条「实例化后的新约束」**（合并几何丢描边壳+尾焰、enemyShip 成死代码）；⑦§8 待优化点重写（下一梯队 BatchedMesh/matrixAutoUpdate 全覆盖、观感回归、难度曲线、**验证链路重建建议拉高**）。
- 第五部分更新：§5.1 核查表加「落地状态」列（✅4 项/🟡1 项 matrixAutoUpdate 部分/⬜5 项）；§5.3 路线图加「状态」列并**新增 2 行**（重建验证链路 ★★★★☆、恢复描边尾焰 ★★★☆☆）；§5.5 执行顺序标完成度；**新增 §5.6 本期落地实施记录**（A 两项技术债/B 子弹/C 敌人/D 水晶/E 验证情况+6 条待人工确认清单+2 条已知取舍）。
- 顺带修文档事实错误：meshes 17 key 原写 `boss_a/boss_b`，实测为 `boss_core/boss_arm/boss_spine`（`node -e` 加载 assets/meshes.js 枚举确认），改为实测列表；`index.html 约 203KB`→`204.5KB`。
- 校验：8 个 markdown 表格列数一致（0 mismatch）；交叉引用 §4.3/§5.6-A~E/§7.4/§7.7/§5.4-1/§5.4-2 均有对应目标。

### 任务 #101（完成）：敌人实例化描边壳回归（16:33）
- 续 #99：实例化敌人合并几何时丢了反向描边壳（cel 观感折损），本期用**第二层 `BackSide` 实例化实例**补偿。
- `build/p6_enemies.js` `Enemies.init`：每 variant 增建 `this._outline[vk] = new InstancedMesh(geo, MeshBasicMaterial{color:0x05070d, side:BackSide}, 300)`（复用同几何，frustumCulled=false，DynamicDrawUsage，renderOrder=-1）；`Enemies._flush`：每活跃敌写 body 矩阵后，再用 `sc*1.06` 写 outline 矩阵（同位置/朝向），末尾对 `_outline[*]` 单独 set needsUpdate。`clear` 借 `_flush` 前的 count 归零已覆盖（循环里 `this._insts[k].count=0; this._outline[k].count=0`）。
- 玩家/僚机/BOSS 仍是单 Group（Gfx.ship 自带描边），不受影响；**仅朝向相关尾焰仍取舍丢失**（单 InstancedMesh 难表达随机型旋转尾焰），记入 §7.7 / §5.3 末行 / §5.6-C。
- 校验：p6 `node --check` 过；`tools/build.mjs` 重建 index.html **205.5KB**；grep 产物 `_outline`×5、`BackSide`×10、`enemyBodyGeo`×2 进产物；sync_doc.py 重同步交付文档至 **284.8KB** 并改 §7.7/§5.3/§5.6-C「描边已补、尾焰仍取舍」、尺寸 204.5→205.5KB，8 表 0 错位、p6 逐字嵌入校验通过。
- ⚠️ 仍未运行时验证（puppeteer 损坏）：人工双击须确认描边壳观感（尤其 1.06× 是否合适、orbiter/splitter 自旋下轮廓是否干净）。

## 18:08 扩张方案第一刀（A 主题 / B 标签共鸣 / H 难度曲线）
- 用户贴《完整优化方案总纲》(A-J 九类)，授权「你看着改 + 记得备份」。选落地低风险高价值地基：A 主题改名 + B 标签共鸣 + H 难度曲线。
- 备份：已建 `build_bak_20260813_180838/` + `index_bak_20260813_180838.html`（cp -r，未删）。
- A 主题：p7 `W_INFO` 5 武器改名（轨道切割环/等离子电弧/量子飞轮/等离子灼焰/重力脉冲，key 不动）；p6 `Enemies.SPEC` 加三支舰队派系注释。
- B 共鸣：p7 新增 `Synergy` 模块（TIERS 六标签 3/6/9/12 阶梯 + mods + refresh/tierInfo/activeList）；13 武器加 `tags`；6 choke point 接线（Enemies.damage/Boss.damage/Player.heal/Player.speed/Player.armor/Weapons.rollCrit,critMul）；Progress.reset+applyCard 调 refresh；选卡/装备栏显标签进度。预留未消费 mods（ctrl/elem/vuln/projSpeed/fireRate/pierce/summonDmg/summonRate/dronePlus/onHitHeal）供后续阶段。
- H 难度：p8 新增 `Game.hpMulAt(w)`（多项式1+0.063w^1.8 + 指数阻尼 exp(min(3.2,(w-12)0.12)) + 上限900，无尽×1.35；w=10≈4.98 对齐旧线性）；`waveSpec` 改用 hpMulAt + 按波缓存；spdMul 无尽 2.2、elite 上限 0.25、w>15 注入 phaser/turret/sniper 复合编队；`Boss.spawn` maxHp 接入同曲线；start 清空 `_waveCache`。
- 验证：8 分片 `node --check` 全过；build.mjs 重建 `index.html` = **212.6KB**；新增 `tools/test_synergy.mjs`（vm 桩加载 p7，零浏览器）18 项断言全过（四套共鸣阶梯数值/默认值/难度曲线对齐/单调/封顶/无尽更陡/tierInfo）。
- 文档：`sync_doc.py` 重同步至 **286.2KB**（p7 逐字嵌入校验通过）；新增 §6 三条 ✅ + §5.7 落地记录（A/B/H 函数级明细 + 验证 + 待续）。
- 待续（下一刀）：B 剩余武器槽 4→6 + swap 卡 UI（待 Phase C 新武器铺满避免软锁）；C/D/E/F/G/I 各阶段；验证链路重建（puppeteer 仍坏，test_synergy 仅覆盖纯数学）。

## 18:25 扩张方案第二刀 · C① 新武器安全批（4 把，复用现有系统）
- 用户确认 A/B/H 第一刀收尾后，问「还有什么优化」，经 AskUserQuestion 选定 **C① 新武器安全批**（4 把低风险复用型新武器）。
- 备份：已建 `build_bak_20260813_182925/` + `index_bak_20260813_182925.html`（cp -r，未删）。
- 实现（全复用、零新实体系统）：
  * `frost`(energy,5级)：`bullets.fire` 青色贯穿弹 `{frost:true}`；p4 命中 `e.frostLv++/frostT=0.5`；p6 `damage` 冻结期(`frostLv>0&&frostT>0`)×1.5；`_flush` 霜蓝 `lerp(0x8fe3ff)`。
  * `meteor`(heavy,5级)：锁定 `pickTargets/nearestTo` 密集区；`Asteroids.geos` 建坠落 Mesh(y=22)+橙警示圈，自管 `Weapons.meteors[]`(t 倒计 1.0s 后 `Enemies.splash` AoE+World.shake)。
  * `swarm`(barrage,5级)：`pickTargets(S.n)` 取最近 n 敌，逐个 `Bullets.missile(...,{scale:0.5,splash,spd:14})` 齐射。
  * `storm`(energy,5级)：`Enemies.pool.active` 随机抽 `S.strikes` 敌，对每个 `_chainBeam({x,z,sky:true},e)`+天空雷击视觉+`Enemies.damage`。`_chainBeam` 支持 `p1.sky` 起点 y=18。
  * 4 武器全进 `W_INFO`(name/icon/desc/tags)→`Progress` 卡片池自动进三选一；Audio2 加 frostZap/meteorWarn/meteorFall/stormCrack。
- 变量避冲突：4 分支用 `fr/mt/sw/st`（火焰分支已占 `fl`），重命名后 `node --check` 通过。
- 验证：8 分片 `node --check` 全过；build.mjs 重建 `index.html` = **218.8KB**(224026 bytes)；新增 `tools/test_synergy.mjs` 第 8 组(新武器标签接入共鸣)→共 22 项断言 ALL PASS；Python `re.findall` 确认 4 武器各 5 级数据完整。
- 文档：`sync_doc.py` 重同步至 **297.4KB**；§6 加 C① ✅ 条目、§5.7 标题改「+ C① 新武器 第二刀」、末尾新增 C 段落(4 武器落点/frost/meteor/swarm/storm 机制/W_INFO/Audio2/验证)。
- ⚠️ 仍待人工双击 `index.html` 确认 4 把新武器运行时观感/手感（puppeteer 损坏，无自动化回归）。
- 下一步：C② 剩余 7 把（需新实体系统）/ B 武器槽 4→6 / 验证链路重建。

## 19:?? 扩张方案第三刀 · C② 新武器实体批（7 把，需新实体系统）
- 用户选「C② 剩余 7 把（黑洞/相位护盾/光子跳弹/牵引光束/旋转相阵/太空雷阵/纳米修复——需新实体系统）」，全部落地。
- 实现（各自管实体池，无新全局系统、架构不变）：
  * `blackhole`(energy)：`Weapons.blackholes[]` Group(暗球+紫吸积环)，逐帧环自转、半径内敌向中心吸附(`pull·dt` 限幅)+`Enemies.damage(...,false)` 静默 DOT；`life` 到期 `Enemies.splash(×0.7,×2.2dps)`+爆发+移除。
  * `phase`(medical)：`Player.phaseMesh`(青色 Sphere 力场) + `phaseHp/phaseMax/phaseT`；`takeDamage` 顶部先于护甲承伤、吸满免伤、破盾清零。
  * `photon`(precise)：`Weapons.photons[]` Mesh(v=40)，命中 `bounces--`、改向最近未命中敌续飞、限次/出界消失。
  * `tractor`(heavy)：常驻 `tractorMesh`(开柱面, opacity 呼吸)，半径内敌聚向玩家(距>3才拉)+`slowK=0.3`+静默 DOT+内向粒子。
  * `rotor`(barrage)：`rotorNodes[]`(6 个 Octahedron 节点) 环绕旋转，每节点 `cd` 对接触敌 `Enemies.damage`。
  * `mine`(barrage)：`minePool[]`(28 槽预建)，`cd` 到点在玩家周围布 n 颗，0.6s 激活后自转、敌近(2.2+r)即 `Enemies.splash` 爆炸、寿命上限 10s。
  * `nano`(medical)：`cd` 到点 `Player.heal(S.hp)`+绿十字+虫群粒子+`Audio2.nano()`。
  * 静默伤害：p6 `damage(e,dmg,crit,hx,hz,fx)` 第 6 参 `fx!==false` 才弹字/火花/击退/音；否则仅 `if(e.hp<=0)kill`。`e.hitT=Math.max(...,0.1)`。
  * Audio2 加 `blackhole/phase/photon/tractor/rotor/mine/nano` 7 音效（p3:480-486）。
  * W_INFO 7 条目(p7:343-349) + `Progress.describe` 7 `else if` 选卡文案(p7:494-506)。
- 变量避冲突：7 分支用 `bh/ph/pt/tr/rt/mn/na` 前缀，重命名后 `node --check` 通过。
- 验证：7 JS 分片 `node --check` 全过；build.mjs 重建 `index.html` = **237.5KB**(243201 bytes)；`tools/test_synergy.mjs` 第 9 组(新武器标签接入共鸣)→共 **9 组 31 项断言 ALL PASS**(energy→t1, medical→t3, precise→t1, heavy→t1, barrage→t3, activeList≥5, W_INFO 含 7 条)；同步 `类土豆兄弟_源码与策划书.md` 至 **318.3KB**(§6 武器数 13→24 + §5.7-D C② 段落)。
- ⚠️ 仍待人工双击 `index.html` 确认 7 把新武器运行时观感（尤其黑洞吸附+坍缩、相位护盾承伤、牵引聚敌、相阵节点、雷阵触发）。puppeteer 损坏无自动化回归。
- 下一步：C 全部 11 把已落地 → **B 武器槽 4→6+swap UI**（C 已铺满可做，防软锁）→ D 敌人+变异 → E 僚机索敌 → F 玩家行为 → G 场景危害 → I 美化。
