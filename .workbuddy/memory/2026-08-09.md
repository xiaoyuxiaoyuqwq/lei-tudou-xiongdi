# 2026-08-09 工作日志

## 《星渊幸存者 / STARFALL SURVIVORS》游戏项目

### 已完成
- **原型 `index.html`（HTML + three.js r160 UMD，单文件双击即玩）**：18 模块 IIFE 架构，29/29 项无头自检通过（Puppeteer + Chrome SwiftShader）。涵盖：自动索敌开火、键盘走位、屏幕外环形刷怪、空间哈希碰撞、对象池、经验晶体+磁力、三选一升级卡、僚机编队+阵型切换、波次时间缩放、HUD、BOSS 三阶段、通关/无尽/死亡结算、重开清理。
- **自检脚本 `tools/check.mjs` 修复**：原 "BOSS 阶段切换" 1 项 FAIL 为测试时序竞争（盲等 800ms 未赶上固定步长帧）。改为轮询 Boss.phase 后 → 29/29 全过（phase=2, 血量比 0.622）。游戏逻辑本身经 `tools/debug_boss.mjs` 独立验证正确。
- **策划书 `策划书.md` + `策划书.html`**：7 章（核心循环 / 敌人 / 战机成长 / 僚机舰队 / 掉落 / 关卡难度 / UI美术）+ 第 8 章附加创意建议（阵型切换、精英词缀、通关结算、进化树、临时buff、无尽赛季）含取舍理由。数值全部对齐原型内 CFG/WEAPONS/PASSIVES/WINGTYPES。

### 关键数值模型（原型已实装）
- 玩家：HP100 移速12 伤10 射速2.5 暴5%/1.5x 拾取3.0 无敌0.6s
- 4 敌+mini+精英+BOSS(12000hp/3阶段)；刷怪 cap=min(40+9w,160) iv=max(0.18,1.10-0.06w) hpMul=1+0.18w+0.02w²
- 武器 cannon/missile/laser/aura Lv1-5 + 进化（绑定 rate/crit/speed/hp 被动 Lv5 + 进化核心）
- 僚机 striker/warden/howitzer，DPS 软上限 0.25×主武器
- 标准局 15 波（W5/W10 精英，W15 BOSS）→ 解锁无尽

### 模型整体优化（晚场）
- **目标**：建模都需要整体优化 → 把单一图元（cone/box/dodeca）升级为**组合式霓虹飞船**。
- **做法**：在 `Gfx` 加共享部件库 `parts`（fuselage/wing/fin/barrel/ring/spike/pod/hub/disc/core/turret，每个只建一次），新增 `Gfx.build(kind,color)` 与 `Gfx.buildBoss()` 工厂返回 `{g:Group, mats:[可闪白材质]}`。
- **重塑实体外观**：
  - 突击者=尖镖+双掠翼+立尾 / 游隼=扁平机身+掠翼 / 狙击手=长管炮舰 / 孢子母舰=球荚+尖刺环+白核 / 孢子=小核
  - 精英=核心+旋转环+10 尖刺+白核 / BOSS=核心+3 层环+8 炮塔环+光晕
  - 玩家=主机身+双掠翼+立尾+座舱 / 僚机三型各异（突击=小镖/守护=圆盘+环/重炮=炮舰）
- **兼容改造**：`_dress` 用 `e.built` 守卫避免重复重建；闪白 / 复色改为遍历 `e.mats`（替代原 `e.body.material.color`，因 `e.body` 现为 Group）。
- **验证**：自检 29/29（FPS 64→52，因对象数 3→~5/敌；测试 BOSS 阶段测量加 force-PLAYING 循环防 LEVELUP 暂停干扰）。
- **截图脚本**：`tools/shot_models.mjs`（强制 PLAYING + 隐藏弹窗 + 关闭磁力拾取），输出 `shot_models.png` 可视化验证。
