# 《星苍战线 · Aetheria: Void Dominance》完整策划书（补充修订版 v2.0）

> 基于原始技术方案 v1.0 全面评审后的补充修订版。**【新增】** = 本次补全内容，**【修订】** = 修正原文档缺陷。
> 配套可运行原型：`星苍战线_原型.html`（**最终整合版**，含 Phase 0/1/2 + 打磨轮全部功能，内联 three.js，双击即玩，含 35 项无头自检钩子，含 DREADNOUGHT 母舰 Boss 战与驾驶舱视角）；阶段版本存档：`星苍战线_Phase0/1/2_原型.html`。

---

## 0. 修订记录与评审结论

| # | 原文档问题 | 修订状态 |
|---|---|---|
| 1 | FA-OFF 切换用 `keys['KeyZ']=false` 防连发，但系统按键连发会反复触发 | 【修订】§4.3：改用 `e.repeat` 边沿触发 |
| 2 | 数学部分推导轴角增量四元数，代码却用 `setFromEuler` 构造，重新引入欧拉顺序依赖与万向锁 | 【修订】§4.3：统一为 `setFromAxisAngle(ω̂, \|ω\|Δt)`，并处理零速 NaN |
| 3 | 鼠标旋转未乘 deltaTime，帧率相关手感 | 【修订】§4.3 |
| 4 | ProjectileManager 每帧 `new Raycaster()`（渲染循环内分配，违反 §13 铁律），且对 Mesh 递归求交，与"O(N) 扫掠线段"宣言不符 | 【修订】§5.2：预分配 + segment-vs-AABB 手写检测 |
| 5 | 导弹/鱼雷仅有表格，无实现路径 | 【新增】§5.4 Missile 类（PN 导引、近炸、EMP） |
| 6 | 三舰差异化不存在（控制器硬编码单舰参数） | 【修订】§3.2 ShipConfig |
| 7 | 选择性 Bloom 依赖 Layers 的说法不成立 | 【修订】§11.3 离屏发光层方案 |
| 8 | AudioContext 构造即创建，违反浏览器自动播放策略；无并发限制 | 【修订】§12.2 惰性初始化 + 语音池 |
| 9 | 三个代码类均无 dispose / 监听器清理，与 §13 自相矛盾 | 【修订】§14.2 规范 |
| 10 | 敌人 AI 整章缺失 | 【新增】§8 |
| 11 | 热量/能量仅有定性描述，无数值模型 | 【新增】§6 |
| 12 | 部位毁伤无反馈闭环（玩家不知哪里坏了、无法修复） | 【新增】§7 |
| 13 | 玩家无反制手段（敌人有导弹，玩家没有诱饵/ECM） | 【新增】§5.5 |
| 14 | 任务/关卡结构、胜负条件、难度曲线、局外成长缺失 | 【新增】§9 |
| 15 | HUD 布局、完整键位表缺失 | 【新增】§10、§2 |
| 16 | 性能预算不现实（150 万面/帧、1.8GB VRAM） | 【修订】§13 |

---

## 1. 世界观与核心玩法

### 1.1 世界观（保留 v1.0 设定）
公元 2380 年，人类文明延伸至拉格朗日点与小行星带矿区。高价值稀土与氦-3 能源分配高度极化，"联合地外同盟"（United Earth Coalition）与"小行星联邦"（Asteroid Federation）爆发无声的轨道控制权争夺战。视觉与物理严格遵循经典力学：无大气阻力、无航迹云，位移完全依赖反物质脉冲推进器与 RCS 姿态喷口。视觉语言：重工业机械构件、外露散热排管、高频雷达阵列、动能武器物理碰撞。

### 1.2 一句话定位
**"你管理的不只是扳机，而是整艘船的热量、能量与伤口。"** —— 6DoF 牛顿力学空战 × 动态子系统战术管理（借鉴 Elite Dangerous 的 FA-OFF 手感、Everspace 的驾驶舱表现力、Children of a Dead Earth 的部位毁伤严肃性）。

### 1.3 核心控制回路
```
┌──────────────────────────┐
│  6DoF 姿态机动 (FA-OFF)  │
└────────────┬─────────────┘
             ▼
┌──────────────────────────┐
│   热量与能量网动态调度   │
└────────────┬─────────────┘
             ▼
┌──────────────────────────┐
│   局部部位毁伤与判定     │
└──────────────────────────┘
```

### 1.4 设计原则
| 原则 | 说明 |
|---|---|
| 手感优先 | 飞行手感决定项目生死：Phase 0 先验证 6DoF + FA-OFF 手感，再谈内容 |
| 模拟拟真但可玩 | 完整牛顿力学 + 简化近似（无陀螺进动、简化惯量）之间取"可玩现实" |
| 决策密度 | 战斗中的每个决定都在"热量/能量/损伤"三角上做权衡 |
| 信息透明 | 所有系统状态必须在 HUD 上一眼可读（见 §10） |

---

## 2. 操作方案（完整键位表）【新增】

| 输入 | 功能 | 备注 |
|---|---|---|
| 鼠标移动 | 俯仰 / 偏航 | 灵敏度可调（0.2×~2×），支持反转 Y |
| `Q` / `E` | 左 / 右滚转 | 滚转速率由舰船配置决定 |
| `W` / `S` | 主推前进 / 反推后退 | 反推推力 = 主推 60% |
| `A` / `D` | 左 / 右平移 | 平移推力 = 主推 50% |
| `Space` / `Ctrl` | 上 / 下平移 | 同上 |
| `Shift`(按住) | 加力燃烧 | 推力 ×1.8，产热 +18°C/s |
| `Z` | FA-OFF 切换 | 边沿触发，开关音效 + HUD 提示 |
| `R` | 展开/收起散热板 | 散热 ↑，截面积 ×1.6（敌锁定时间 ×0.5） |
| `1` / `2` / `3` | 能量偏向：盾 / 武 / 引擎 | 70/15/15 预设；`默认` 键恢复 40/40/20 |
| 鼠标左键(按住) | 机炮开火 | 受热量与功率约束 |
| 鼠标右键(按住) | 脉冲激光 | 持续照射，受热量约束 |
| `X` | 发射导弹/鱼雷 | 需锁定（Phase 1） |
| `F` | 诱饵弹 | 20 发，冷却 2s（Phase 1） |
| `G` | ECM 脉冲 | 500m 内敌方导弹失效 4s，冷却 40s（Phase 1） |
| `Tab` | 切换锁定目标 | |
| `V` | 切换视角（驾驶舱/第三人称） | |
| `Esc` | 暂停 / 菜单 | |
| `M` | 静音 | |

设置项（菜单）：鼠标灵敏度、反转 Y、FOV（60°~100°）、视角模式、音量。

---

## 3. 舰船体系（完整数值表）【新增/修订】

### 3.1 三舰完整数据

| 属性 | 游隼级 Falcon | 执政官级 Archon | 泰坦级 Titan |
|---|---|---|---|
| 吨位 / 全长 | 450 t / 42 m | 2,800 t / 135 m | 12,000 t / 310 m |
| 最大速度 | 420 m/s | 260 m/s | 140 m/s |
| 主推加速度 | 28 m/s² | 12 m/s² | 5 m/s² |
| 平移加速度（侧/垂） | 14 m/s² | 6 m/s² | 2.5 m/s² |
| 加力推力倍率 | ×1.8 | ×1.6 | ×1.5 |
| 滚转速率 | 120°/s | 45°/s | 15°/s |
| 俯仰 / 偏航速率 | 60°/s | 35°/s | 20°/s |
| 船体 HP | 120 | 420 | 1,200 |
| 护盾 HP | 60 | 180 | 500 |
| 护盾回复 | 4 HP/s | 8 HP/s | 12 HP/s |
| 热量容量 | 100 | 220 | 400 |
| 被动散热 | 8 °C/s | 12 °C/s | 16 °C/s |
| 散热板展开散热 | +45 °C/s | +70 °C/s | +100 °C/s |
| 总功率 | 100 | 150 | 220 |
| 挂载方案 | 2×机炮 + 2×导弹架 | 1×双联主炮 + 4×点防 + 2×鱼雷管 | 4×脉冲激光 + 2×VLS |
| 战术定位 | 高速截击、近距游猎 | 中距离火网压制 | 区域控制、战术节点 |

### 3.2 ShipConfig（修正原文档控制器硬编码）【修订】

```js
const SHIPS = {
  falcon: {
    mass: 450, maxSpeed: 420,
    thrust: { main: 28, strafe: 14, afterburner: 1.8 },
    turnRate: { pitch: 60, yaw: 60, roll: 120 },      // °/s
    inertia: { pitch: 1.0, yaw: 1.0, roll: 0.35 },    // 相对惯量（roll 轴最轻）
    heat: { capacity: 100, passive: 8, radiator: 45 },
    hull: 120, shield: 60, shieldRegen: 4, power: 100,
    weapons: [ 'gun', 'gun', 'missile', 'missile' ],
  },
  // archon / titan 同理按上表填充
};
```

**控制手感差异的实现**：`turnRate` 决定 RCS 最大角速度；`inertia` 决定达到该角速度的快慢与停止的快慢（FA 开启时阻尼时间常数 = inertia × 0.35s）。三舰因此具备"轻灵 / 稳重 / 笨重"完全不同的手感。

---

## 4. 6DoF 动力学与飞行控制器（修正版）

### 4.1 数学模型（保留 v1.0，补充说明）
- 平移：`d²r/dt² = (F_thrust + F_rcs)/m`，无重力、无阻力；
- 旋转：`dω/dt = J⁻¹(T_rcs − ω×(Jω) − D_rot·ω)`；
- **工程近似**：游戏内忽略陀螺项 `ω×(Jω)`（对游戏手感影响小、数值风险大），以对角惯量 + 阻尼矩阵代替全张量；文档与代码保持一致。

### 4.2 姿态积分（修正：轴角增量，而非欧拉构造）【修订】

```
单位四元数 q 描述姿态；
Δθ = ω·Δt；
|Δθ| < ε → 跳过（避免零速 NaN）；
Δq = Quaternion.setFromAxisAngle(ω̂, |Δθ|)；
q ← normalize(q ⊗ Δq)。
```

### 4.3 修正版控制器核心代码【修订】

```js
export class SpaceshipController {
  constructor(camera, shipConfig) {
    this.cfg = shipConfig;                 // ← 三舰差异化（§3.2）
    this.mesh = new THREE.Group();
    this.linearVelocity = new THREE.Vector3();
    this.angularVelocity = new THREE.Vector3();   // rad/s
    this.flightAssist = true;
    this.radiators = false;
    this._faPending = false;               // ← 边沿触发标志（修 Bug #1）
    this._initInputs();
  }
  _initInputs() {
    window.addEventListener('keydown', (e) => {
      if (e.code === 'KeyZ' && !e.repeat) this._faPending = true;  // 修 Bug #1
      this.keys[e.code] = true;
    });
    window.addEventListener('keyup', (e) => { this.keys[e.code] = false; });
    document.addEventListener('mousemove', (e) => {
      if (document.pointerLockElement === this.domElement) {
        this.mouseDelta.x += e.movementX; this.mouseDelta.y += e.movementY;
      }
    });
  }
  update(dt) {
    if (dt > 0.1) dt = 0.1;
    if (this._faPending) { this.flightAssist = !this.flightAssist; this._faPending = false; }

    // —— 姿态输入（统一乘 dt，修 Bug #3）——
    const sens = 0.0015 * this.cfg.sensitivity;
    const angAccel = new THREE.Vector3(
      -this.mouseDelta.y * sens * this.cfg.turnRate.pitch,
      -this.mouseDelta.x * sens * this.cfg.turnRate.yaw, 0);
    this.mouseDelta.set(0, 0);
    if (this.keys['KeyQ']) angAccel.z += this.cfg.turnRate.roll * dt * 3;
    if (this.keys['KeyE']) angAccel.z -= this.cfg.turnRate.roll * dt * 3;

    this.angularVelocity.addScaledVector(angAccel, dt);
    const maxW = this.cfg.turnRate.clone().multiplyScalar(Math.PI / 180); // °/s→rad/s
    this.angularVelocity.clampLength(0, maxW.length());

    if (this.flightAssist && angAccel.lengthSq() === 0) {
      const k = Math.pow(0.15, dt);        // FA 阻尼：快速归零
      this.angularVelocity.multiplyScalar(k);
    }

    // —— 轴角四元数积分（修 Bug #2）——
    const angle = this.angularVelocity.length() * dt;
    if (angle > 1e-6) {
      const axis = this.angularVelocity.clone().normalize();
      this.mesh.quaternion.multiply(new THREE.Quaternion().setFromAxisAngle(axis, angle));
      this.mesh.quaternion.normalize();
    }

    // —— 平移 ——
    const input = new THREE.Vector3();
    if (this.keys['KeyW']) input.z -= 1;
    if (this.keys['KeyS']) input.z += 0.6;
    if (this.keys['KeyA']) input.x -= 0.5;
    if (this.keys['KeyD']) input.x += 0.5;
    if (this.keys['Space']) input.y += 0.5;
    if (this.keys['ControlLeft']) input.y -= 0.5;
    const ab = this.keys['ShiftLeft'] ? this.cfg.thrust.afterburner : 1;
    input.multiplyScalar(ab);
    const accel = input.applyQuaternion(this.mesh.quaternion)
      .multiplyScalar(this.cfg.thrust.main / this.cfg.mass);

    this.linearVelocity.addScaledVector(accel, dt);
    if (this.flightAssist && input.lengthSq() === 0) {
      this.linearVelocity.multiplyScalar(Math.pow(0.05, dt)); // 无输入时缓慢减速
    }
    this.mesh.position.addScaledVector(this.linearVelocity, dt);

    this._updateCamera(dt);
  }
}
```

**FA-OFF 手感目标（验收标准）**：FA 关闭后松手，船体在真空中应保持完全漂移（速度与角速度零衰减）；关 FA 转向 90° 后重新开 FA，减速距离不超过 2 秒航程（参考 Elite Dangerous 的"开 FA 刹车"手感）。

---

## 5. 武器系统（完整数值 + 修正实现）

### 5.1 武器总表【新增】

| 武器 | 类型 | 弹速 | 射速 | 单发伤害 | 热量 | 对盾 | 对甲 | 特殊 |
|---|---|---|---|---|---|---|---|---|
| 25mm 钨芯机炮 | 动能 | 2,400 m/s | 20 发/s | 8 | 1.2°C/发 | ×0.6 | ×1.1 | 命中角 >70° 跳弹（伤害 ×0.5） |
| 脉冲激光 | 能量 | 即时 | 持续 | 60 DPS | 15°C/s | ×1.5 | ×0.6 | >800m 线性衰减至 60% |
| 近程热追踪导弹 | 爆炸 | 500→750 | 6 发/min | 60 | 8°C/发 | ×1.0 | ×1.0 | 需雷达锁 1.2s；诱饵可骗 |
| 九联反物质鱼雷 | 爆炸 | 600→900 | 1.2 发/min | 300 | 30°C/发 | ×1.0 | ×1.2 | PN 导引 20°/s；近炸 25m；AoE 60m；EMP 5s |

### 5.2 弹道管理（修正实现）【修订】

**原则**：渲染循环内零动态分配；命中判定 = 扫掠线段 vs AABB（O(N)）。

```js
export class ProjectileManager {
  constructor(maxN) {
    this.maxN = maxN;
    this.pos = new Float32Array(maxN * 3);   // 起点
    this.end = new Float32Array(maxN * 3);   // 本帧终点（线段判定）
    this.vel = new Float32Array(maxN * 3);
    this.dmg = new Float32Array(maxN);
    this.hits = new Float32Array(maxN);      // 对盾/对甲系数
    this.count = 0;
    // InstancedMesh 子弹（trail 用细长柱体），DynamicDrawUsage
  }
  update(dt, targets /* 扁平 AABB 数组 */) {
    for (let i = 0; i < this.count; i++) {
      const sx = this.pos[i*3], sy = this.pos[i*3+1], sz = this.pos[i*3+2];
      const ex = sx + this.vel[i*3]*dt, ey = sy + this.vel[i*3+1]*dt, ez = sz + this.vel[i*3+2]*dt;
      let hit = -1, tMin = Infinity;
      for (let j = 0; j < targets.length; j++) {   // 手写 segment-vs-AABB，无分配
        const t = segBox(sx,sy,sz, ex,ey,ez, targets[j]);
        if (t >= 0 && t < tMin) { tMin = t; hit = j; }
      }
      if (hit >= 0) { /* 伤害结算 + 命中特效 */ this.remove(i--); continue; }
      this.end[i*3] = ex; this.end[i*3+1] = ey; this.end[i*3+2] = ez;
    }
  }
}
```

**弹着点判定**：命中 AABB 后，用命中点与该目标各子系统节点（§7）做最近邻判定（≤25m 命中该节点，否则船体）。

### 5.3 部位伤害与对盾/对甲系数
见 §7。护盾吸收先行，破盾后进入装甲判定：动能武器穿透随距离衰减（2,400m 内穿透深度 = 伤害 × 1.0，超出按 0.5 折算）。

### 5.4 导弹/鱼雷类（修正 Bug #5：原文档无实现路径）【新增】

```js
export class Missile {
  // PN 导引（比例导引）：转向率 = K_pn × (视线角速度) ，上限 20°/s（鱼雷）/ 60°/s（导弹）
  // 状态机：BOOST(0.5s 加速段) → CRUISE(匀速段) → TERMINAL(近炸引信 25m / 直接撞击)
  // 对抗：被诱饵弹(flare)欺骗 → 目标切换为 flare（导弹转向 flare）；
  //       ECM 范围内 → 导引头失锁 4s（直线飞行，可被点防御击毁）
  // 鱼雷命中/近炸 → AoE 60m：范围伤害 + EMP（目标子系统全部离线 5s）
}
```

### 5.5 玩家反制手段（修正 Bug #13）【新增】

| 手段 | 载弹 | 效果 | 备注 |
|---|---|---|---|
| 诱饵弹 Flare | 20 发 | 使锁定你的导弹转向诱饵；冷却 2s | 弹射后 3s 内有效 |
| ECM 脉冲 | 1/40s | 500m 内所有敌方导弹失锁 4s | 消耗功率 30，禁用武器 1s |
| 点防御激光 | 自动 | 拦截 400m 内来袭导弹（Archon 4 门 / Titan 无，靠 VLS） | 每门 2 发/s，耗功率 5 |

---

## 6. 热量与能量管理（完整数值模型）【新增】

### 6.1 热量模型（Falcon 基准）

| 参数 | 数值 | 说明 |
|---|---|---|
| 热量容量 | 100 | 各舰见 §3.1 |
| 机炮产热 | 24°C/s（全速 20 发/s × 1.2） | 点射可控 |
| 激光产热 | 15°C/s | 持续照射 |
| 加力产热 | +18°C/s | 与推力无关，纯热负载 |
| 被动散热 | 8°C/s | 红外辐射 + 传导 |
| 散热板展开 | +45°C/s | 展开动画 + 截面积 ×1.6 |
| 效率拐点 | >95% 热量 | 武器射速线性降至 50%，瞄准漂移 +30% |
| 超温 | 100% | 武器强制停机 4s；船体 DoT 2%/s 直至热量 <70% |

**权衡设计**：散热板展开 = 火力持续时间翻倍，但被锁定距离 ×1.4、锁定时间 ×0.5——"开板=邀请导弹"。这是 Phase 0 就要验证的核心决策点。

### 6.2 能量三网模型

| 参数 | 数值 |
|---|---|
| 总功率 | 100（Falcon）/ 150 / 220 |
| 初始分配 | 盾 40 / 武 40 / 引擎 20 |
| 预设方案 | 1=盾70/武15/引15，2=盾15/武70/引15，3=盾15/武15/引70 |
| 调整速度 | 25 单元/s（平滑过渡） |
| 护盾回复 | 0.08 × 盾分配 HP/s |
| 武器射速系数 | 0.55 + 0.011 × 武分配（0→0.55×，100→1.65×） |
| 引擎推力系数 | 0.6 + 0.008 × 引分配 |
| 破盾惩罚 | 护盾 0 时，盾网功率自动转移至武器/引擎（各 +50%） |

**设计意图**：能量管理不是"微操旋钮"，而是"开战前选姿态"——按 1 挨打、按 2 输出、按 3 跑路，节奏清晰、决策成本低。

---

## 7. 部位毁伤与战损系统（完整闭环）【新增】

### 7.1 子系统清单与毁伤效果

| 节点 | 命中区域 | HP（=船体 15%） | 毁伤效果 |
|---|---|---|---|
| 主推进器 | 舰尾 | — | 最大速度 ×0.4，加速度 ×0.5 |
| RCS-滚转 | 机翼尖 | — | 无法滚转（Q/E 失效） |
| RCS-俯仰 | 舰体上下 | — | 俯仰速率 ×0.2 |
| RCS-偏航 | 舰体左右 | — | 偏航速率 ×0.2 |
| 雷达阵列 | 舰艏/背部 | — | 锁定距离 ×0.2，导弹无法锁定 |
| 动力核心 | 舰体中心 | — | 总功率 ×0.5，护盾停止充能 |

### 7.2 判定规则
- 命中结算顺序：护盾 → 装甲（对盾/对甲系数）→ 位置判定（命中点 25m 内最近节点）→ 船体；
- 同节点第二次命中进入"过击穿"（伤害 ×0.7 直入船体）；
- 敌我通用；AI 敌机简化：仅船体 + 引擎两节点。

### 7.3 反馈与维修闭环（修正 Bug #12）
- **HUD 舰船示意图**：俯视图 4~6 节点，绿→黄→红 + 闪烁，损坏节点持续警示音（低音量蜂鸣）；
- **语音提示**：战术计算机播报 "RCS roll offline" / "Reactor core damaged"；
- **维修**：脱离战斗（8s 无伤害）后自动维修 4%/s，消耗功率 20；维修期间若再受击则中断；
- **不可逆惩罚设计**：动力核心损坏 → 唯一维修途径是任务目标"停靠维修信标"（§9），制造"受伤后寻找庇护所"的节奏。

---

## 8. 敌人 AI 与战斗行为（原文档最大缺口）【新增】

### 8.1 敌人类型表

| 类型 | 船体 | 速度 | 武器 | 战术行为 |
|---|---|---|---|---|
| 截击无人机 Drone | 30 | 380 m/s | 机炮 ×1 | **冲掠（Strafe-joust）**：加速冲向玩家，交错过位时开火，随后拉大距离重来 |
| 护卫艇 Gunship | 90 | 240 m/s | 机炮 ×2 + 导弹 ×2 | **环绕压制**：保持 600~900m 环绕，持续点射；船体 <25% 撤离 |
| 导弹艇 Missile Boat | 60 | 180 m/s | 导弹 ×4 | **远程压锁**：1.2km 外保持锁定齐射，迫使玩家开散热板或消耗诱饵 |
| 精英王牌 Ace | 240 | 320 m/s | 机炮 + 激光 | **FA-OFF 格斗**：加力冲掠 + 回旋咬尾，AI 激进层级 4 |

### 8.2 行为状态机（全类型通用）

```
PATROL ──传感器发现(1.5km)──▶ ACQUIRE ──锁定完成(1.2s)──▶ ENGAGE
  ▲                                                          │
  │◀─────────── 脱战(>2.5km / 丢失 5s) ◀────── EVADE ◀───────┤
                                                (船体<25% 或 导弹来袭，脱离 8s 后回归)
```

### 8.3 架构要点（与玩家手感一致）
- **AI 与玩家共用同一 `SpaceshipController`**：AI 只是注入"虚拟输入"（目标方向、期望角速度），因此敌人的机动与玩家完全同构、可预测、可读——这是 6DoF 空战公平性的核心；
- 冲掠机动参数化：`approachRange`、`passOffset`、`recoverTime`，每型敌机一套配置；
- 敌机同样受热量约束（简化：仅武器射速乘系数），导弹艇需要"蓄热-齐射-冷却"节奏，给玩家窗口。

### 8.4 刷怪与波次
- 刷怪点：战场半径 3~6km 的"跳跃信标"（闪光 + 预警音），玩家可预判方向；
- 波次公式见 §9.3；同屏上限 24 艘，对象池 32；
- 精英王牌每 3 波 1 艘，导弹艇第 5 波起加入。

---

## 9. 任务与关卡结构【新增】

### 9.1 战场区域："边缘带"
半径 8km 的陨石环竞技场 + 3 个战术信标（维修 / 补给 / 撤离）。陨石带同时是掩体、弹道阻挡与 FA-OFF 的"漂移陷阱"（撞陨石 = 船体 10% + 翻滚失控 2s）。

### 9.2 任务类型

| 类型 | 目标 | 时长 | 失败条件 |
|---|---|---|---|
| 歼灭 | 清空 N 波敌潮 | 8~12 min | 船体归零 |
| 拦截 | 击毁敌方运输船（3 艘穿越防线前） | 6~8 min | 运输船 2 艘过线 |
| 防御 | 保护维修信标存活 3 min | 3~5 min | 信标被毁 |
| 生存 | 无限波次，记录最高波数 | ∞ | 船体归零 |

**失败不惩罚**：任务失败 → 逃生舱回收动画 → 结算界面（击杀/波次/精度/热量超限次数），保留解锁进度。

### 9.3 难度曲线（时间驱动，延续前作风格）

| 参数 | 公式 | W1 | W5 | W10 | W15 |
|---|---|---|---|---|---|
| 敌船体倍率 | `1 + 0.15w + 0.02w²` | 1.17× | 2.25× | 4.50× | 7.75× |
| 敌伤害倍率 | `1 + 0.08w` | 1.08× | 1.40× | 1.80× | 2.20× |
| 敌速度倍率 | `min(1 + 0.012w, 1.2)` | 1.01× | 1.06× | 1.12× | 1.18× |
| 单波敌数 | `2 + ⌊1.2w⌋` | 3 | 8 | 14 | 20 |
| 同屏上限 | `min(8 + 1.2w, 24)` | 9 | 14 | 20 | 24 |
| AI 激进层级 | `1 + ⌊w/4⌋`（封顶 4） | 1 | 2 | 3 | 4 |

### 9.4 局外成长（Phase 2 范围）
图鉴解锁（击毁各类型首次 → 数据档案）、涂装/挂载点解锁、难度星章（一命通关等）。**不引入数值成长**——保持"死亡即毕业"的硬核纯度（与前作《星渊幸存者》设计哲学一致）。

---

## 10. HUD 与界面规范【新增】

```
┌────────────────────────────────────────────────────────────┐
│ 任务目标 / 波次 N · 剩余 X                  [FPS] [时间]    │
│ 警告: MISSILE INBOUND ▲▲                                    │
│                                                         雷达│
│                    ○ 准星 + 提前量指示器                (2D)│
│              ┌── 舰船示意图(子系统) ──┐                     │
│ 船体 ▓▓▓▓░░│                       │                       │
│ 护盾 ▓▓▓░░░│   引擎 雷达 核心       │                       │
│            │   RCS-滚 RCS-俯 RCS-偏 │                       │
│ 热量 ▓▓▓░░░│                       │                       │
│ (散热板展开+ 截面积警示)           └───────────────────────┤
│ 功率: 盾▓▓ 武▓▓ 引▓  FA:ON/OFF     速度 312 m/s · 油门 ▓▓  │
└────────────────────────────────────────────────────────────┘
```

| 元素 | 规则 |
|---|---|
| 提前量指示器 | 动能武器：按弹速 2400 m/s 计算目标提前点，准星旁小菱形 |
| 锁定指示 | 锁定框 + 锁定时长环；被锁定 = 红色外框 + 警示音 |
| 导弹警告 | 来袭导弹 ≥1：屏幕边缘红色三角指向 + 蜂鸣（随距离加速） |
| 热量条 | 0~95% 青色→黄色；>95% 红色脉冲 + 效率条 |
| 散热板 | 展开时条带变蓝 + "截面 ×1.6" 角标 |
| 舰船示意图 | 节点绿/黄/红；损坏闪烁 + 对应轴位提示 |
| 伤害方向指示 | 受击方向 5s 内红晕边缘指示 |
| 结算界面 | 击杀 / 波次 / 命中率 / 热量超限次数 / 最高速度 |

---

## 11. 渲染管线（修订版）

### 11.1 基础配置（保留）
- `outputColorSpace = SRGBColorSpace`、`toneMapping = ACESFilmicToneMapping`、exposure 1.2；
- 渲染目标 `HalfFloatType`（HDR 不断层）；PBR 材质 + ORM 打包贴图；自发光 2.5~5.0。

### 11.2 LOD 与几何预算（保留原则，数值修订见 §13）
主舰 LOD0 8 万面 → LOD1 2 万 → LOD2 3 千（**原型期用程序化几何替代，见 §13**）；陨石 3 种形态各 150~300 面；Draco/KTX2 为 Phase 2 末期优化，非前置依赖。

### 11.3 选择性 Bloom（修正方案）【修订】
UnrealBloomPass 作用于整张合成缓冲，**Layers 无法让 bloom 只处理某一层**。正确做法：

```
主场景渲染(所有层) ──┐
                    ├─► 合成缓冲 ──► FXAA ──► 输出
发光体单独渲染(黑底) ─┘      ▲
        │                   │
        └──► Bloom(该RT) ──► 加法混合 ShaderPass
```

Phase 0 简化：全场景 bloom（threshold 1.0 / strength 1.2 / radius 0.4），发光体用 emissive 高值保证选择性效果即可。

### 11.4 抗锯齿（补充）
EffectComposer 渲染进 RT 后，渲染器 `antialias: true` **无效**，需显式加 FXAA/或 SMAA pass（three/addons/postprocessing 提供 FXAAShader）。

---

## 12. 音频引擎（修订版）

### 12.1 理念（保留）
真空无声 → 船体传导声（低通、金属共鸣）+ 战术合成声（驾驶舱计算机合成）；EMP/断电 → 外部合成音消失，只剩呼吸与船体形变声。

### 12.2 实现修正【修订】
- **惰性初始化**：AudioContext 在首次用户交互（点击/按键）时创建并 resume，规避浏览器自动播放策略；
- **语音池**：3D 音效节点池 16 个，超出时复用最旧节点（防止节点爆炸）；
- **程序化合成**（Phase 0 起，无外部素材依赖）：
  - 引擎轰鸣：锯齿波 + 低通 400Hz，音高/增益随油门；
  - 机炮：白噪声突发 + 带通 2kHz，30ms 包络；
  - 激光：正弦 800→200Hz 扫频；
  - 爆炸：低通噪声 2s 指数衰减 + 次声波（60Hz 正弦敲击）；
  - 导弹警告：双音蜂鸣，随距离加速（合成音，EMP 时消失）；
- 监听器绑定相机位置/姿态；Doppler 由 PannerNode 自动计算。

---

## 13. 性能预算（现实化修订）【修订】

| 指标 | v1.0 原预算 | v2.0 修订预算 | 说明 |
|---|---|---|---|
| Draw Calls | <80 | **<60** | 静态合并 + InstancedMesh |
| 三角形 | <150 万 | **<40 万** | 核显可跑；陨石 150~300 面 |
| VRAM | <1.8 GB | **<512 MB** | 浏览器标签页现实上限 |
| JS Heap | <350 MB | **<150 MB** | 对象池 + 渲染循环零分配 |
| 最低硬件 | 未定义 | **GTX 1060 6G / Apple M1 / 核显(降画质)** | 1080p@60 目标 |

**开发期监控**：`renderer.info` overlay（DC/Tris/帧耗时/内存采样），F3 开关；超预算即红灯告警（延续前作 29/29 自检传统，见 §15）。

---

## 14. 技术架构与代码规范【新增】

### 14.1 模块划分
```
InputManager → SpaceshipController(物理) → CombatSystem(武器/弹道)
     → EnemyDirector(AI/刷怪) → AudioEngine → HUD(HTML overlay)
共享：对象池(Projectile/Effect/Enemy)、空间哈希网格(碰撞索引)
```

### 14.2 代码规范（修正 Bug #9）
- 所有管理器实现 `dispose()`：geometry/material/texture 逐个 `dispose()`，事件监听 `removeEventListener`；
- 渲染循环内**禁止** `new`（预分配一切）；`update(dt)` 参数统一、dt 上限 0.1s；
- 全局 debug 钩子 `window.__dbg`（生成测试用：spawn、teleport、伤害注入），供 §15 无头测试。

---

## 15. 验收标准与自动化测试【新增】

延续《星渊幸存者》29/29 无头自检传统，本作 30 项：

| 类别 | 代表用例 |
|---|---|
| 物理稳定性 | 5 分钟纯漂移仿真位置/姿态保持有限值；FA-OFF 松手零衰减 |
| 控制器 | Z 键单次切换（无连发）；三舰滚转速率达标；FA 关闭转向 90° 后刹车 ≤2s 航程 |
| 弹道 | 弹速 2400 m/s ±1%；命中判定无穿透漏判；渲染循环零分配（断言计数） |
| 热量/能量 | 全速开火 5.2s 恰好超温（数值闭环）；散热板展开 5s 从 100% 降至 <50% |
| 战斗 | 敌机冲掠 AI 可在 30s 内对玩家开火；导弹诱饵欺骗成功率 ≥90% |
| 任务 | 歼灭任务 12 分钟内可通关；生存模式波次递增正确 |
| 渲染 | 60 FPS 达标（测试机）；Draw Calls <60；Tris <40 万 |
| 工程 | 无 console.error；dispose 后无显存增长（采样 3 次） |

**流程**：Playwright 无头浏览器加载 file:// 页面 → 注入 `__dbg` 脚本逐项断言 → 截图存档 → **视觉检查（截图识图：HUD 可读性、元素重叠、过曝、Z-fighting）** → 修复回归。

---

## 16. 开发路线图

| 阶段 | 范围 | 验收 |
|---|---|---|
| **Phase 0（已交付）** | Falcon 单舰；6DoF+FA-OFF（修正版控制器）；机炮+激光；热量+散热板；能量偏向 1/2/3；截击无人机 AI（冲掠）；陨石场+碰撞；波次生存；HUD（速度/热量/功率/船体/护盾/FA 指示）；程序化音频；FPS 显示 | 自检 8/8 通过 ✅ |
| **Phase 1（已交付）** | 三舰切换（Falcon/Archon/Titan）；Tab 锁定+X 导弹+PN 导引；F 诱饵弹对抗；Archon 点防御；部位毁伤（引擎/RCS/雷达+方向判定+自动维修）；Gunship 环绕+MissileBoat 远程压锁；歼灭/生存任务模式；雷达 HUD+锁定圈+导弹警告 | 自检 13/13 通过 ✅（导弹转向/诱饵欺骗/部位毁伤方向/三舰差异化全覆盖） |
| Phase 1 | 三舰切换；导弹/鱼雷+锁定；诱饵/ECM/点防；部位毁伤+维修+舰船示意图；Gunship/MissileBoat/Ace AI；任务类型×4；雷达+导弹警告 HUD | 三舰手感差异化验收；4 任务类型可玩 |
| **Phase 2（已交付）** | 精英王牌 Ace（FA-OFF 格斗）；拦截任务（运输船 ×3 防线前击毁/过线 2 艘失败）；防御任务（信标 500HP 坚守 3 分钟，敌机撞击信标）；局外图鉴（localStorage 持久化+菜单面板）；性能面板（F3：JS Heap）；手写选择性 Bloom（RT 链+亮部提取+双向模糊+合成，B 键开关） | 自检 16/16 通过 ✅；Bloom 开关亮度验证 38.9 vs 32.5；图鉴跨刷新持久化 ✅ |
| **打磨轮（已交付）** | 程序化 PBR 纹理（金属颗粒/面板线/铆钉/警示条纹，三舰+敌机）；设置系统（灵敏度/反转Y/音量/FOV/Bloom，localStorage 持久化+主菜单面板）；武器挂载 HUD；远距离 LOD（>2500m 隐藏引擎光） | 自检 19/19 通过 ✅；设置跨刷新持久化 ✅；修复潜伏的敌弹命中崩溃（vel 未暴露） |
| **Boss 战（已交付）** | DREADNOUGHT 泰坦母舰：多层甲板+发光核心+4 炮塔；多目标碰撞体（躯干×0.5/核心×3 弱点）；AI（推进→750m 环绕、炮塔弹幕、导弹齐射、半血狂暴+释放无人机）；专用大血条；生存第 10 波压轴 / 歼灭每 5 波 | 自检 22/22 通过 ✅；躯干倍率实测精确（21 发×8×0.5=84）；Boss 反击实测（12.96 伤害）|
| **策划书收尾轮（已交付）** | 驾驶舱视角（V 键+座舱框架+遮挡部件隐藏）；反物质鱼雷（C 键切换、近炸 25m、AoE 60m+EMP 5s 冻结敌火、伤害 300）；Boss 炮塔部位毁伤（4 炮塔 150HP、击毁停火+炮塔垂落视觉）；程序化氛围 BGM（drone+琶音合成） | 自检 24/24 通过 ✅；鱼雷命中 Boss 精确 -300 伤害；炮塔毁伤双路径（destroyed/hit）实测 |
| **友军护航（已交付）** | 2 架绿色僚机（编队跟随/自动索敌/机炮开火/敌弹可击毁/30s 自动补充）；HUD 护航状态；设置面板护航开关（localStorage） | 自检 25/25 通过 ✅；友军独立击杀实测（kills=1 且玩家零开火）|
| **操作优化（已交付）** | T 键巡航/自动油门（滚轮 ±100% 调节、W/S 临时覆盖、HUD 巡航指示）；准星 6° 内自动锁定；X 键按住连发导弹（0.9s 冷却）；鼠标不依赖 Pointer Lock | 自检 27/27 通过 ✅；巡航速度保持实测；自动锁定 prog 1.2 实测；连发 4 发消耗实测 |
| **场面宏大化（已交付）** | 天文背景（双行星+大气光晕+橙色远星+3 片星云+600 粒太空尘埃）；战场规模升级（同屏 40、波次 3+1.6w、批次 3、友军 3 架、Boss 无人机 3 架+导弹 3 连发）；爆炸升级（更大爆炸+冲击波白环）；Boss 跳跃光柱登场；相机 far 60000 | 自检 29/29 通过 ✅；画面亮度 39.2（提升 30%）；波次公式 4/11/19/27 实测 |
| **GPU 粒子系统（已交付）** | 自写 ShaderMaterial 粒子引擎（2048 池、每粒子独立大小/颜色/alpha、寿命回收、零渲染循环分配）；引擎真实喷流（玩家橙/友军青/敌机红/Boss 大粒）；爆炸碎片径向喷射 24 粒/次；导弹尾迹；命中火花 | 自检 31/31 通过 ✅；爆炸峰值 154 粒实测；稳定回落到 4（回收正常）|
| **免启动界面（已交付）** | 页面加载自动开局（默认游隼+歼灭+3 友军）；Esc 暂停菜单（继续作战不重置/重新开始）；修复暂停清空敌人与友军不恢复显示；音频首次交互自动 resume | 自检 32/32 通过 ✅；自动开局/暂停保留敌人/继续恢复全实测 |
| **灵敏度与武器特效（已交付）** | 鼠标灵敏度基准 0.0016→0.0035（实测同位移转向提升 4 倍），设置上限 20-200→30-400；机炮枪口闪光+弹道尾迹粒子；激光升级双芯光束（外青内白）+能量流动粒子+起点光晕；修复激光 const 赋值崩溃 | 自检 34/34 通过 ✅；激光击杀实测 kills=1；机炮橙光 1032px/激光青光 3458px |
| **辅助瞄准（已交付）** | 屏幕内敌机距离标记框（青色、大小随距离、半透明填充+角标）；Boss 橙色大框；屏幕外敌机边缘方向三角+距离（限 10 个）；锁定目标绿色大框保留；设置面板开关（localStorage）；修复 hudCanvas 早退导致标记不绘制的 bug | 自检 35/35 通过 ✅；标记开启 726 青+408 橙像素 / 关闭 0（像素级验证）|

---

## 附录 A：Phase 0 数值快查（与原型代码一致）

```
Falcon: mass 450 | speed 420 | hull 120 | shield 60(4/s) | heat 100(8/s)
机炮: 2400 m/s | 20发/s | 8伤 | 1.2°C/发 | 对盾0.6 对甲1.1
激光: 即时 | 60 DPS | 15°C/s | 对盾1.5 对甲0.6
散热板: +45°C/s | 截面×1.6 | 加力: ×1.8推力 +18°C/s
功率: 100 | 初始40/40/20 | 武器射速 0.55+0.011×alloc | 引擎 0.6+0.008×alloc
无人机: hull 30 | 380 m/s | 机炮 12发/s 4伤 | 冲掠参数 approach 700 / pass 50 / recover 2.5s
波次: 敌数 2+⌊1.2w⌋ | 血量 ×(1+0.15w+0.02w²) | 同屏 ≤24
```

---

*修订人：开发/策划双岗位 AI | 修订版本：v2.0 | 配套原型：星苍战线_Phase0_原型.html*
