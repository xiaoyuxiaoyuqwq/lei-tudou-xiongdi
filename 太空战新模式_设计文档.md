# 《星陨幸存者》太空战新模式 · 设计文档

> 配套项目:`index.html`(单文件 three.js,双击即玩)
> 本文档规划三个新模式:**缠斗空战 / 舰队指挥 / 星图战役**,外加一个公共框架(M0)。
> 原则:所有模式**复用现有引擎**(对象池/InstancedMesh/空间哈希/卡池/波次导演/WebAudio/无头验证),只新增"规则层"。

---

## 0. 总览

| 模式 | 核心循环一句话 | 输入增量 | 参考 | 预估工作量 |
|---|---|---|---|---|
| 缠斗空战 (arena) | 手动瞄准 + 提前量 + 锁定导弹的 1vN 缠斗 | 走位 → **走位+瞄准** | Star Fox / Freelancer / Squadrons | 3~5 天 |
| 舰队指挥 (fleet) | 指挥 6 艘战舰的编队/集火/技能 | 走位 → **命令** | Starsector / Homeworld / Battlevoid | 4~5 天 |
| 星图战役 (campaign) | 星图节点抉择 + 多场战斗 + 商店/事件的长局 | 单局 → **旅程** | FTL / Slay the Spire / Hades | 5~7 天 |

**公共框架 M0(0.5~1 天)**:模式注册表 + 种子系统 + 计分排行榜 + 菜单模式入口 + 构建接入 + 无头验证骨架。
**推荐实施顺序**:M0 → 缠斗 v1 → 舰队 v1 → 战役 v1(每阶段结束跑验证+截图+文档同步,沿用项目惯例)。

---

## 1. 公共框架 M0

### 1.1 模式注册表

新分片 `build/p9_modes.js`(加入 `tools/build.mjs` 的 `PARTS` 数组):

```js
/* ===== 模式注册表:每个模式 = init/reset/frame/over 四件套 ===== */
const Modes = {
  arena:    { id:'arena',    title:'缠斗空战', init(){}, reset(){}, frame(dt){}, over(win){} },
  fleet:    { id:'fleet',    title:'舰队指挥', init(){}, reset(){}, frame(dt){}, over(win){} },
  campaign: { id:'campaign', title:'星图战役', init(){}, reset(){}, frame(dt){}, over(win){} },
};
```

`Game.mode`(默认 `null` = 经典幸存者)在 `Game.start` 前由菜单模式卡设定;`Game.frame(now)` 的 `PLAYING` 分支改两行:

```js
if (Game.mode) { Modes[Game.mode].frame(dt); }
else { /* 现有幸存者更新链 */ }
```

### 1.2 种子系统(战役/每日挑战的地基)

- 新 `Util.setSeed(s)`:内部 `mulberry32` PRNG,`Util.rand` 改为读 `Util._rng`;无种子时回退 `Math.random`。
- **待办**:全局 grep `Math.random` 直调(约几十处,主要在 p3 FX / p6 刷怪 / p7 卡池),统一走 `Util.rand`——这是种子化唯一有风险的改动,列为 M0 任务。
- 每局开始生成种子并显示在 HUD;设置菜单加"种子输入框"(粘贴种子复现同一局)。

### 1.3 计分与排行榜(缠斗/舰队通用)

- 新 `Score` 模块:`record(mode, score, meta)` 写 `localStorage['sts_scores']`(每模式 top5),结果页复用 `showResult` 加一行"本局种子 + 分数"。

### 1.4 菜单入口

`p1_shell.html` 主菜单加"模式"行(复用 `.ship-cards` 卡片样式,三张卡 + 默认高亮"经典幸存者")。

### 1.5 验证骨架

- `tools/repro_mode.mjs`:复制 `repro_boss.mjs` 的 vm 桩模式(桩 WebGLRenderer/DOM/Audio),跑每个模式的 `init→reset→600 帧→over` 全生命周期,四模式参数(normal/hell/coop)回归。
- 每个模式配 `tools/shot_<mode>.mjs` 截图脚本(沿用 `shot_*.mjs` 摆场景截图惯例)。

---

## 2. 模式 A:缠斗空战(arena)

### 2.1 核心循环

```
鼠标瞄准 → 机炮自动连射 → 敌机提前量闪避/狗斗 → 锁定导弹终结
   → 连击倍率滚分 → 每 90s 王牌机师(Ace)登场 → 击落 Ace = 大分+全修
```

### 2.2 操作表

| 输入 | 功能 | 说明 |
|---|---|---|
| 鼠标移动 | 瞄准 | 射线投影到地面 y=0 平面,准星跟随 |
| WASD | 平移机动 | 复用现有移动模型 |
| Shift/空格 | 加力 | 复用冲刺(时长放宽到 0.4s),镜头 FOV +5° |
| 鼠标左键(按住 0.8s) | 锁定导弹 | 锁定环闭合后松开发射,备弹 6、装填 3s/发 |
| E | 诱饵弹 | 2 发,敌导弹重锁假目标(复用 warden 拦截思路) |
| Tab | 辅助瞄准开关 | **默认开**:准星吸附视锥内最近敌(保低门槛);关 = 纯手动 |

### 2.3 武器(精简 3 槽,区别于幸存者 4 槽卡池)

| 槽 | 武器 | 数值(Lv1 基准) |
|---|---|---|
| 固定① | 机炮 | 伤 8 / 射速 8/s / 弹速 **55**(快于狙击手 40)/ 散布 0.05 |
| 固定② | 锁定导弹 | 伤 45 / 弹速 36 / 转向 3.0 / 备弹 6 |
| 可选③ | 三选一:光束(持续)/ 近炸榴弹 / 无人机(自动补刀) | 复用现有武器表 `Weapons.TABLE` 精简 |

### 2.4 敌机表(模式私有 AI,不进 STAGES)

| 敌机 | HP | 速度 | 行为 | 特点 |
|---|---|---|---|---|
| 游猎机 | 30 | 11 | chase+weave | 基础杂兵 |
| 拦截机 | 45 | 14 | 蛇形机动+预判射击 | 会用你的提前量思路打你(lead=dist/40×1.2) |
| 炮舰 | 120 | 6 | 远距炮塔+缓慢逼近 | 血厚,导弹优先目标 |
| 导弹艇 | 60 | 8 | 8s 一次锁定你 | **锁定警告音+红色警告**,逼你交诱饵或打断 |
| **王牌 Ace** | 260 | 13 | 每 90s 登场 | 见 2.5 |

### 2.5 Ace(王牌机师)——本模式的核心亮点

- 行为:蛇形走位 + **每 1.5~2s 随机 90° 急转**(反提前量)+ 预判射击 + 8s 周期锁你导弹。
- 实现:60% 复用 `Enemies.SPEC` 的 weave/lead 逻辑,新增一个"急转计时器"字段即可,不需要新 AI 框架。
- 击落奖励:500 分 × 连击倍率 + 全队回 20 盾 + 横幅播报(复用 `waveBanner`)。
- 视觉:金色涂装(复用精英 `0xffe0a0`)+ 名字条(复用 `#bossName` 样式,小号版)。

### 2.6 生存与计分

- 两段血量:**盾**(自动回充,破盾 3s 裸奔)→ **结构**(不回充)。盾上限 60、结构 100。
- 计分:击杀 = 100 × 连击倍率;3s 内再击杀连击 +1(上限 ×8);Ace = 500 × 倍率;结算按时间给尾分。
- 节奏:标准局 5 分钟(3 阶段:侦察小队 → 编队战 → 王牌+护航),之后转无尽计分。

### 2.7 HUD 布局

```
┌──────────────────────────────────────────────┐
│  盾 ▓▓▓▓ 60/60  结构 ▓▓▓ 100/100  连击 ×4   │ ← 顶部
│  ✛ 提前量环(目标前方青色环)    ◇ 锁定框       │ ← 战场
│  导弹 ▓▓▓▓▓▓○(6)  诱饵 ▓▓(2)  时间 2:34      │ ← 左下
│  ⚠ ACE INBOUND ─── (金色横幅,复用 waveBanner)│
└──────────────────────────────────────────────┘
```

### 2.8 MVP 验收标准

鼠标瞄准+提前量环+机炮+锁定导弹+2 种敌机+1 只 Ace+计分结算,10 分钟实玩无崩溃,击杀/连击/锁定/诱饵全部可感知。

### 2.9 实现落点与风险

- 落点:`Input.aim()`(p3 新增,射线投影)、提前量环(3D 圆环池 ×8,青色,`MeshBasicMaterial`+Additive)、导弹复用 p5 missile 参数、Ace 复用 p6 weave/lead + 精英涂装、计分走 Score 模块。
- **风险(手感三要素)**:提前量公式、弹速、转向率是缠斗手感的核心,预留 1 个调参日;起步参考值:机炮弹速 55、转向 3.2 rad/s、Ace 急转间隔 1.75s。鼠标瞄准在触屏不可用(游戏本就键盘定位,可接受;辅助瞄准默认开兜底)。

---

## 3. 模式 B:舰队指挥(fleet)

### 3.1 核心循环

```
暂停下指令(战斗冻结) → 选阵型/集火/放技能 → 恢复 1x/2x 速度观战
   → 敌舰队压境(3 波) → 击破敌旗舰 = 胜 / 我方旗舰沉没 = 败
```

### 3.2 操作表

| 输入 | 功能 |
|---|---|
| 鼠标移动/点击 | 移动舰队锚点(舰队整体平移目标) |
| 鼠标右键(点敌舰) | 集火标记(全队切换目标,敌背后受击 +50%) |
| 1 / 2 / 3 | 阵型:楔形(全队伤 +15%)/ 环形(受击 −15%)/ 横列(射速 +10%) |
| 空格 | 暂停下指令(战斗冻结,复用 LEVELUP 暂停模式) |
| Q / E / R | 技能:护盾墙(40 能量)/ 火力齐射(30)/ 紧急跃迁(60) |

### 3.3 友舰单位表(6 艘)

| 舰种 | 数量 | HP | 速 | 伤 | 射速 | 定位 |
|---|---|---|---|---|---|---|
| 截击机 interceptor | 2 | 60 | 16 | 6 | 2.5/s | 缠斗清杂 |
| 巡洋舰 cruiser | 2 | 140 | 9 | 14 | 0.8/s | 主力输出 |
| 支援舰 support | 1 | 80 | 10 | — | 治疗 8/3s | 维修波技能(全队 +15 HP,CD 20s) |
| 轰炸机 bomber | 1 | 90 | 6 | 40(对旗舰 60) | 0.25/s | 点杀旗舰 |

**你的旗舰**(座舰,沉没即败):HP 400 / 速 12 / 自动炮塔伤 20。

### 3.4 敌舰队(3 波)

| 波 | 组成 | 备注 |
|---|---|---|
| W1 | 6 艘:拦截×2 + 炮舰×2 + 蜂群×2(V 形编队入场) | 熟悉指令 |
| W2 | 8 艘:混编 + 1 艘支援舰(会维修) | 必须先集火支援 |
| W3 | 敌旗舰(HP 3000,弱化 Boss 结构,3 座可破坏炮塔)+ 4 艘护航 | 炮塔击破 = 弹幕减半 |

### 3.5 能量与技能

- 全局能量 100,自然回复 1.2/s,击杀 +5;技能消耗见 3.2。
- 战术深度(Starsector-lite):**侧翼夹击**(敌背后受击 +50%,检查朝向点积)、阵型克制、**撤退保存兵力**(下波保留残舰,维修耗时→资源决策)。

### 3.6 实现落点(复用率最高的模式)

| 现有系统 | 复用方式 |
|---|---|
| `Wingmen` 三行为+阵型(p5) | 友舰 = 大号僚机;编队槽位点计算直接复用 |
| 友军炮台 AI(p6 Hazards GOOD 区) | 旗舰自动炮塔 |
| `director` 刷怪(p8) | 加"编队模板"刷怪(V 形中队/炮塔护卫组) |
| `Boss` 结构(p7) | 敌旗舰 = 弱化 Boss(HP 3000,`userData.turrets` 3 座可破坏) |
| `Grid`/`Pool` | 友舰+敌舰 ≈ 40 单位,性能无压力 |
| `bossBar` | 敌旗舰血条 |

### 3.7 MVP 验收标准

6 舰 3 种 + 阵型切换 + 集火 + 暂停指令 + 敌旗舰 + 胜负判定,完整打完一局(胜或败)无异常。

### 3.8 风险

- **指挥反馈慢**:新手挫败来自"指令没效果"。缓解:暂停下指令 + 集火时敌舰闪红框提示 + 指令音效反馈(已拍板:不做倍速,保持 1x 节奏)。
- 友舰 AI 表现:首版直接复用 wingmen 行为,不做复杂寻路;旗舰锚点移动用指数平滑。

---

## 4. 模式 C:星图战役(campaign)

### 4.1 核心循环

```
星图 4 列×3 行,每列 3 选 1 → 节点:战斗/精英/商店/维修/事件
   → 战斗 = 90s 幸存者小局(自动索敌,构筑跨节点保留)
   → 残骸货币买武器/维修 → 终点 BOSS → 评级 S/A/B/C + 图鉴/徽章
```

### 4.2 星图与节点

- 结构:起点 → 4 列(每列 3 节点选 1)→ 终点 BOSS。节点权重:战斗 45% / 事件 20% / 商店 15% / 维修 10% / 精英 10%。
- **跨节点保留**:武器/被动等级/僚机/HP 全部带进下一场战斗(核心差异——单局构筑变成"旅程构筑",参考 FTL 与 Hades)。
- 战斗节点时长 90s;难度按**全局波次累积**换算(首个战斗节点 = W1,之后每节点 +2 波,复用 `hpMulAt` 曲线)。

### 4.3 残骸货币(Scrap)

| 来源/用途 | 数值 |
|---|---|
| 战斗结算 | 击杀 ×1、精英 ×10 |
| 商店武器 | 60 / 80 / 100(3 选 1,复用选卡 UI) |
| 商店被动卡 | 40 |
| 维修 | 2/HP,回满 60 |
| 事件选项 | 见事件表 |

### 4.4 事件表(8 个,2 选 1 卡,复用 levelup 卡片样式)

| 事件 | 选项 A | 选项 B |
|---|---|---|
| 废弃空间站 | 搜刮:+40 残骸(20% 遇伏 −15 HP) | 绕过:无事 |
| 星图异常 | 下一战斗节点敌 +20% HP | 但掉落 ×1.5 |
| 赏金任务 | 本节点击杀结算 ×1.5 | 跳过 |
| 维修无人机 | 花 20 残骸回 40 HP | 不收 |
| 跃迁风暴 | 商店价 −25% | 但维修效率 −50% |
| 友军信号 | 下一战斗节点 +1 支援炮台 | 跳过 |
| 陨石雨 | 下一战斗节点全程陨石危害(敌我皆受影响) | 跳过 |
| 能量核心 | 立即 +1 进化核心(若有 Lv5 武器可当场进化) | 换 30 残骸 |

### 4.5 轻 meta(不破坏"死亡即毕业"的纯净性)

- **图鉴**(`localStorage['sts_codex']`):敌人种类击杀、武器使用次数、事件首次选择——纯收集,无数值。
- **徽章**:首次通关 / 无商店通关 / 满血通关 / 30 分钟内通关,结算页展示。
- 评级:综合残骸 + 剩余 HP + 通关时间 → S/A/B/C,只给正反馈不惩罚。

### 4.6 HUD

```
┌──────────────────────────────────────────────┐
│ ●─●─●─○─●  残骸 128  节点 3/4  盾 60/60     │ ← 顶部星图进度(横条圆点)
│              (当前列高亮,复用 mini-map 画法) │
│  事件弹窗(2 选 1 卡,复用 levelup 遮罩)       │
└──────────────────────────────────────────────┘
```

### 4.7 MVP 验收标准

星图 3×4 + 战斗/商店/维修/事件四类节点 + 残骸货币 + 终点 BOSS + 结算页,完整通关一局(约 25~35 分钟)或中途死亡结算,无崩溃。

### 4.8 实现落点与风险

- 落点:星图 = DOM 圆点行(便宜);事件/商店卡 = 复用 levelup 卡片 DOM;局内 run 状态 = `Game.run` 对象(内存即可);图鉴/徽章 = Settings 同款 localStorage 模式;终点 BOSS = 复用 Boss 结构按全局波次缩放。
- **风险**:跨节点构筑平衡(玩家带着 Lv5 武器进下一场)→ 战斗节点难度按累积波次已留余量,首版多调一轮数值。
- **局内存档(已拍板:首版就做)**:用 `localStorage['sts_campaign_run']` 存 `Game.run`(星图种子、当前节点索引、残骸、HP、武器/被动等级、事件标记)。序列化只存纯数据不存对象引用;读档做版本号校验,损坏则丢弃并提示(复用 Settings 的 try/catch 兜底模式)。菜单加"继续战役"入口,有档才显示。

---

## 5. 排期与交付惯例

| 阶段 | 内容 | 交付检查(沿用项目惯例) |
|---|---|---|
| M0 | 框架+种子+计分+菜单+验证骨架 | `_syntax_check.mjs` 通过 · `build.mjs` 重建 · `repro_mode.mjs` 三模式空跑无异常 |
| M1 | 缠斗空战 v1 | repro_mode arena 全生命周期 · `shot_arena.mjs` 截图 · 文档同步 |
| M2 | 舰队指挥 v1 | repro_mode fleet 胜/败两路径 · `shot_fleet.mjs` 截图 |
| M3 | 星图战役 v1 | repro_mode campaign 通关/死亡两路径 · `shot_campaign.mjs` 截图 |

每个模式交付前跑:`node tools/_syntax_check.mjs` → `node tools/build.mjs` → `node tools/repro_mode.mjs [mode] [hell] [coop]` → 截图 → 更新本文档与 README。

---

## 6. 决策记录(用户拍板)

| # | 问题 | 决定 |
|---|---|---|
| 1 | 缠斗模式辅助瞄准默认? | **默认开**(Tab 可关),保低门槛 |
| 2 | 舰队模式 2x 倍速? | **不做**,保持 1x 节奏 |
| 3 | 战役模式局内存档? | **首版就做**(localStorage 存 run 状态,见 §4.8) |
| 4 | 实施顺序 | **暂不实施**——本文档作为后续开发依据;动手时按 M0 → 缠斗 → 舰队 → 战役 推进 |
